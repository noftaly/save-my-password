<!DOCTYPE html>
<html lang="fr">
  <head>
    {{> head}}
  </head>

  <body class="d-flex flex-column" data-spy="scroll" data-target=".fixed-top">
    <header>
      {{!-- Puts the right navbar (the 'index' navbar has more menus) --}}
      {{#if (eq title 'Accueil')}}
        {{> navbar index=true}}
      {{else}}
        {{> navbar index=false}}
      {{/if}}
    </header>

    <main>
      {{{body}}}
    </main>

    <footer class="footer mt-auto">
      {{> footer template=true}}
    </footer>
  </body>
</html>
